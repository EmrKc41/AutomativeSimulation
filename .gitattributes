# Normalise line endings in the repository regardless of the checkout platform,
# so a Windows and a Linux contributor never produce a whole-file diff.
* text=auto eol=lf

*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary
